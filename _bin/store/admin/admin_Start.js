db.system.js.save({_id:"admin_Start",value:function (params) {
	var pub=public();
	var C=constant();
	var params=params||{};
	var obj=_get_session(params);
	if(obj.ok){
		return obj;
	}else{
		return obj;
	}
}})
